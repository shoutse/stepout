<%if @position%>
<h1><%=@position.name%></h1>>
<%end%>

<table>
	<%@industries.each do |i|%>
		<tr>
		<td>
			<%=link_to i.name,topics_path(:i=>i.id,:p=>params[:p])%>
		</td>
		</tr>
	<%end%>
</table>


<%= render :partial => "list", :locals => { :topics => @topics } %>